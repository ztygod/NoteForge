# NoteForge 开发约定

## 代码注释

- 每个新增或修改的源代码文件必须在文件最开头添加完整的中文块注释，说明文件职责、边界和设计思路。
- 关键业务规则、跨字段校验、引用解析、兼容性处理和不明显的实现选择必须添加中文注释。
- 所有导出的类型、接口、常量、函数及其字段必须使用中文文档注释说明用途。
- 注释应解释“为什么”和约束边界，避免只是逐行复述代码。

## Course Contract 协议边界

- `course-contract` 只定义课程数据、协议版本和纯验证逻辑，不依赖 React、后端框架、数据库或 NoteForge Python 核心。
- JSON Schema 负责结构约束；TypeScript 验证器负责跨字段引用和业务语义。两者修改时必须保持一致。
- 新增场景类型时，应在独立模块中定义类型和验证器，并由课程验证入口统一调度。
- 验证器应尽可能收集全部问题并返回结构化 `ValidationIssue`，不要在发现首个错误时抛出异常。

## 测试要求

- 新增验证规则必须包含一个通过用例或失败用例。
- 错误代码是稳定的程序接口；修改已有错误代码前应考虑兼容性。
